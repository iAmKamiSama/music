package mPlayer;

public interface MusicPlayerInterface {
	public void generateDisplay(String[] arr);
	//pass in an array to generate the display for which the things will be displayed in
	
	//MAKE a JTextField for user input ALONG WITH a clickable JLabel that passes the 
	//info in the field to Xavier AND wipe it clean
}
